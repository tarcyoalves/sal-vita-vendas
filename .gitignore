node_modules
dist
.env
# Build artifacts — regenerated by the Vercel buildCommand on every deploy.
# Keeping them versioned added tens of thousands of diff lines per build and
# buried real changes during review.
api/bundle.js
sallog/api/bundle.js
*.local
drizzle

.vercel
CREDENCIAIS.txt
# Added by code-review-graph
.code-review-graph/
.claude/worktrees/
.claude/worktrees/
